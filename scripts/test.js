const Pi = 3.14;

/**
 * 
 * @param {Number} a 
 * @param {Number} b 
 * @returns {Number}
 */
const f1 = (a, b) => {
    return a + b;
}

const NoteUI = () => 'test';

export default NoteUI;

export {Pi as p, f1};